

#   Enable mouse mode (for when I have a stroke)

#   The tmux < 2.0 variant
#set -g mode-mouse on
#set -g mouse-select-window on
#set -g mouse-select-pane on
#set -g mouse-resize-pane on

#   The tmux 2.x variant
set -g mouse on

#   So the 256 colors will work
set -g default-terminal screen-256color

#   Set colors of tmux
set -g pane-active-border-fg colour117
set -g status-bg colour117
set -g status-fg black
